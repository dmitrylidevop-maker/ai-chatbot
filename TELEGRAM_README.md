# Telegram Bot Integration

## Описание

Telegram бот предоставляет полный функционал AI чата через Telegram мессенджер с персонализацией, историей диалогов и автоматическим определением языка.

## Возможности

✅ **Персонализированные приветствия** - Бот генерирует уникальное приветствие для каждого пользователя с помощью LLM  
✅ **Автоматическое определение языка** - Отвечает на том языке, на котором написано сообщение (русский, английский, испанский, немецкий, французский, иврит)  
✅ **История диалогов** - Сохранение и использование контекста разговора  
✅ **Профиль пользователя** - Хранение личной информации для персонализации  
✅ **Несколько сессий** - Возможность начать новую сессию с чистой историей  

## Установка

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота:
   - Введите имя бота (например: "My AI Assistant")
   - Введите username бота (должен заканчиваться на `bot`, например: `my_ai_assistant_bot`)
4. BotFather пришлет вам токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
5. Скопируйте этот токен

### 2. Настройка конфигурации

Добавьте токен в файл `.env`:

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_ADMIN_IDS=  # Опционально: ID админов через запятую
```

### 3. Установка зависимостей

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости (aiogram уже в requirements.txt)
pip install -r requirements.txt
```

## Запуск

### Запуск Telegram бота

```bash
./start_telegram.sh
```

Или напрямую:

```bash
python3 telegram_bot.py
```

### Запуск REST API (параллельно с ботом)

В другом терминале:

```bash
./start.sh
```

## Использование

### Доступные команды

| Команда | Описание |
|---------|----------|
| `/start` | Начать общение с ботом, получить персонализированное приветствие |
| `/newsession` | Начать новую сессию чата (предыдущая история сохраняется) |
| `/profile` | Посмотреть ваш профиль и личную информацию |
| `/help` | Показать справку по командам |

### Примеры использования

1. **Начало работы:**
   ```
   /start
   ```
   Бот создаст ваш профиль и поприветствует вас персонализированным сообщением.

2. **Общение:**
   Просто отправьте текстовое сообщение на любом поддерживаемом языке:
   ```
   Привет! Как дела?
   Hello! How are you?
   שלום! מה שלומך?
   ```

3. **Новая сессия:**
   ```
   /newsession
   ```
   Начнет новый диалог, сохранив предыдущую историю в базе данных.

4. **Просмотр профиля:**
   ```
   /profile
   ```
   Покажет вашу личную информацию и предпочтения.

## Архитектура

### Структура файлов

```
chat-bot/
├── telegram_bot.py              # Точка входа для Telegram бота
├── start_telegram.sh            # Скрипт запуска бота
├── app/
│   ├── telegram/
│   │   ├── __init__.py
│   │   └── handlers.py          # Обработчики команд и сообщений
│   └── services/
│       └── telegram_service.py  # Бизнес-логика Telegram интеграции
```

### Компоненты

**TelegramService** (`app/services/telegram_service.py`)
- Управление пользователями (создание, поиск по telegram_id)
- Управление сессиями диалогов
- Генерация персонализированных приветствий
- Обработка сообщений с помощью LLM

**Handlers** (`app/telegram/handlers.py`)
- `/start` - Инициализация пользователя и приветствие
- `/newsession` - Создание новой сессии
- `/profile` - Отображение профиля
- `/help` - Справка
- Обработка текстовых сообщений

### Интеграция с базой данных

Telegram бот использует ту же базу PostgreSQL, что и REST API:

- `users` - Пользователи (автоматически создаются при /start)
- `user_details` - Детали профиля (имя из Telegram)
- `personal_facts` - Личная информация (включая telegram_id)
- `chat_history` - История всех диалогов

Каждый Telegram пользователь автоматически получает:
- Уникальный `user_id` (9-значный ID)
- Факт с ключом `telegram_id` для связи с Telegram аккаунтом
- Username из Telegram
- Full name из Telegram профиля

## Персонализация

### Автоматическая персонализация

Бот автоматически использует:
- **Имя пользователя** из Telegram профиля
- **Username** из Telegram
- **Telegram ID** для идентификации

### Ручная персонализация через REST API

Вы можете добавить дополнительную информацию через REST API:

```bash
# Найдите user_id по telegram_id в базе данных
# Затем используйте REST API для добавления фактов:

curl -X POST "http://localhost:8000/user/{user_id}/facts" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "fact_key": "language",
    "fact_value": "русский"
  }'
```

## Многоязычность

Бот автоматически определяет язык сообщения и отвечает на том же языке:

- **Русский** - Кириллица (А-Я)
- **Иврит** - Еврейские символы (א-ת)
- **Английский** - Английские слова (the, is, are, what, how)
- **Испанский** - Испанские слова (el, la, es, hola)
- **Немецкий** - Немецкие слова (der, die, das, ist)
- **Французский** - Французские слова (le, la, est, sont)

Язык по умолчанию настраивается в `.env`:
```bash
DEFAULT_LANGUAGE=russian
```

## Устранение проблем

### Бот не отвечает

1. Проверьте, что бот запущен: `ps aux | grep telegram_bot`
2. Проверьте токен в `.env`
3. Проверьте логи в терминале

### Ошибка подключения к базе данных

```bash
# Проверьте подключение к PostgreSQL
psql -h 192.168.31.129 -p 5435 -U postgres -d postgres
```

### Ошибка Ollama

```bash
# Проверьте, что Ollama запущен
curl http://localhost:11434/api/tags

# Проверьте, что модель загружена
ollama list
```

### Бот не определяет язык

Убедитесь, что в сообщении достаточно текста для определения языка. Короткие сообщения типа "да", "нет" могут определяться неправильно.

## Docker

Для запуска в Docker:

```bash
# Собрать образ (включает Telegram бота)
./docker-build.sh

# Запустить контейнер с Telegram ботом
docker run -d \
  --name ai-chatbot-telegram \
  --env-file .env \
  -e MODE=telegram \
  ai-chatbot
```

Для переключения между REST API и Telegram ботом в Docker используйте переменную `MODE`:
- `MODE=api` - REST API (по умолчанию)
- `MODE=telegram` - Telegram бот

## Примеры взаимодействия

### Сценарий 1: Первый запуск

```
Пользователь: /start

Бот: 🌟 Привет, Дмитрий! 👋 
Рад познакомиться с тобой! Я твой персональный AI-ассистент.
Чем могу помочь сегодня? 😊
```

### Сценарий 2: Мультиязычность

```
Пользователь: Привет! Расскажи про Python

Бот: Привет! Python - это высокоуровневый язык программирования...

Пользователь: Tell me about JavaScript

Бот: JavaScript is a programming language that...

Пользователь: ספר לי על פייתון

Бот: פייתון היא שפת תכנות ברמה גבוהה...
```

### Сценарий 3: Управление сессиями

```
Пользователь: /newsession

Бот: 🔄 Начата новая сессия! Предыдущая история сохранена.

Пользователь: Привет!

Бот: Привет! 👋 Начинаем с чистого листа. О чём поговорим?
```

## Разработка

### Добавление новых команд

Добавьте обработчик в `app/telegram/handlers.py`:

```python
@router.message(Command("mycommand"))
async def cmd_mycommand(message: Message):
    """Handle /mycommand"""
    await message.answer("My response")
```

### Добавление новых языков

Обновите метод `_detect_language()` в `app/services/ollama_service.py`:

```python
def _detect_language(self, text: str) -> str:
    # Добавьте проверку для нового языка
    if any('\uXXXX' <= char <= '\uYYYY' for char in text):
        return "новый_язык"
```

## Безопасность

- Токен бота хранится в `.env` и не коммитится в Git
- Пароли пользователей хэшируются с помощью bcrypt
- JWT токены для REST API с ограниченным временем жизни
- База данных использует защищенное подключение

## Лицензия

Проект использует те же лицензии, что и основное приложение.

## Поддержка

При возникновении проблем:
1. Проверьте логи: `./start_telegram.sh`
2. Проверьте конфигурацию в `.env`
3. Убедитесь, что все сервисы запущены (PostgreSQL, Ollama)
